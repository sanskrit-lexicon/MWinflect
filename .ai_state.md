# Project Objective: Conservative Cologne question research for MWinflect morphology issues

## ➡️ Next Steps (Queue)

- [ ] For MWinflect #48, research the exact scope of long `A` in `sah`/`zah` compounds before editing code: decide whether MW's nom.sg. `zAw` evidence applies only to nominative/vocative singular, also neuter 1/2/8 singular, or the whole non-vowel-ending paradigm.
- [ ] For MWinflect #46, do not enable `n_1_v div` until an authority confirms the neuter forms; current `m_1_v div` is intentionally partial and `janAv`/`dIv` are listed as irregular/unresolved.
- [ ] For MWinflect #45, verify `KaYj` with Huet or another grammar/source for B-ending and 7p behavior, and obtain a clean Kale p.97 reading for `viSva-sfj` before changing the current PW/MW-backed k-type output.
- [ ] For MWinflect #44, decide whether the labial `stems_problem.txt` entries (`gardaB`, `gfB`, `sute-gfB`, `syUma-gfB`, `ap`, `bahv-ap`, `rIty-ap`, `sv-ap`) should be promoted into the known 1-stem assignment list, and confirm any neuter labial forms before enabling `n_1_p`/`n_1_B` candidates.
- [ ] For MWinflect #43, treat cerebral `z` stems as a batch/authority problem: many m/f examples are implemented, but the large `n_1_z` queue in `stems_problem.txt` needs a rule for which neuter compounds are valid before promotion.
- [ ] Resume with: `cd C:\Users\user\Documents\GitHub\MWinflect\nominals\pysanskritv2\tables` then run `python decline_one.py m_1_h aBI-zah` and `python decline_one.py n_1_h aBI-zah` to compare current short-`a` output against the proposed long-`A` rule.
- [ ] For #46, resume with `python decline_one.py m_1_l kamal`, `python decline_one.py f_1_r gir`, and `python decline_one.py m_1_v div`; if researching the open neuter guess, `python decline_one.py n_1_v div` currently reproduces the failure.
- [ ] For #45, resume with `python decline_one.py m_1_c vAc`, `python decline_one.py n_1_c vAc`, `python decline_one.py m_1_j KaYj`, `python decline_one.py m_1_j viSva-sfj`, and the speculative-neuter probes `python decline_one.py n_1_c su-vfSc` / `python decline_one.py n_1_j rAj`.
- [ ] For #44, resume with `python decline_one.py m_1_B kakuB`, `python decline_one.py f_1_B gfB`, `python decline_one.py f_1_p ap`, `python decline_one.py f_1_p bahv-ap`, plus neuter probes `python decline_one.py n_1_B gardaB` and `python decline_one.py n_1_p ap`.
- [ ] For #43, resume with `python decline_one.py m_1_R su-gaR`, `python decline_one.py m_1_R prAR`, `python decline_one.py m_1_z tviz`, `python decline_one.py m_1_z da-Dfz`, and neuter probes `python decline_one.py n_1_R su-gaR` / `python decline_one.py n_1_z dviz`.
- [ ] If the rule is approved, patch the `Decline_1` h-ending branch used via `nominals/pysanskritv2/tables/decline_file.py` and add focused `decline_checks.txt` examples for at least `aBI-zah`, `jalA-zah`, `niz-zah`, and one `sah` compound.

## 🚧 Current Work-In-Progress (WIP)

- [ ] No source edit active. 2026-06-27 research classified MWinflect #43/#44/#45/#46/#48 as evidence-rich but authority-gated before code changes.

## 🧠 Dev Notes & Hypotheses (Bugs, ideas, context)

- 2026-06-27 candidate matrix:
  - MWinflect #43 "Nominals of 1 stem ending in cerebral consonant": `question`, `trivial`, `domain:morphology`. Local `analyze_1cons.txt` confirms the small non-`z` cerebral inventory: 5 `w`, 1 `W`, 3 `q`, 0 `Q`, and 3 `R` entries; it also shows 190 `z` entries. `stems.py` already seeds `gaR`, `parAR`, and `prAR`, plus many `z` stems (`vfz`, `dviz`, `tviz`, `Dfz`, etc.) in `known_1stems_list`.
  - MWinflect #43 local probes: `python decline_one.py m_1_R su-gaR`, `python decline_one.py m_1_R prAR`, `python decline_one.py m_1_w laGaw`, `python decline_one.py m_1_W saW`, `python decline_one.py m_1_q Iq`, `python decline_one.py m_1_z tviz`, and `python decline_one.py m_1_z da-Dfz` all generate tables; `decline_checks.txt` already records `tviz` and `da-Dfz` as Kale-confirmed examples.
  - MWinflect #43 open gaps: `stems_problem.txt` has no current `w/W/q/R` unresolved rows, but it has a large `z` queue, especially many `n_1_z` compounds. Local `python decline_one.py n_1_z dviz` generates `dviw dvizI dviNzi ...`, but there is no issue-side authority table for blanket neuter promotion. Decision: `ready-for-correction` only after a focused `z`/neuter policy; do not batch-promote every cerebral inventory item from counts alone.
  - MWinflect #44 "Nominals of 1 stem ending in labial consonant": `question`, `trivial`, `domain:morphology`. The issue body only gives the `analyze_1cons.txt` inventory, which local `nominals/pysanskritv2/analysis/analyze_1cons.txt` confirms: 30 `p`, 0 `P`, 1 `b`, 33 `B`, and 7 `m` entries. `nominals/pysanskritv2/stems/stems.py` already has `kakuB` as the one explicit "labial final from Antoine" known 1-stem.
  - MWinflect #44 local probes: `python decline_one.py m_1_B kakuB`, `python decline_one.py f_1_B gfB`, `python decline_one.py f_1_p ap`, `python decline_one.py f_1_p bahv-ap`, `python decline_one.py m_1_b kub`, and `python decline_one.py m_1_m kim` all generate tables through `nominals/pydecl/decline_1cons.py`. The unresolved work is assignment/authority, not a missing labial declension engine.
  - MWinflect #44 open gaps: `stems_problem.txt` still lists `gardaB`, `gfB`, `sute-gfB`, `syUma-gfB`, and the `ap` compounds as problem stems; local neuter probes (`n_1_B gardaB`, `n_1_p ap`) generate speculative-looking `gardamBi` / `ampi` style forms with no issue-side authority table. Decision: `ready-for-correction` only after confirming which labial stems should be promoted and whether neuter labials are valid; do not patch mechanically from frequency counts alone.
  - MWinflect #45 "Nominals of 1 stem ending in palatal consonant": `question`, `trivial`, `domain:morphology`. Local probes confirm the implemented c/j models reproduce the issue's documented current choices: `python decline_one.py m_1_c vAc` gives the k-type `vAk ...`, `python decline_one.py n_1_c vAc` gives `vAk vAcI vAYci ...`, `python decline_one.py m_1_c su-vfSc` gives the w-type `suvfw ...`, `python decline_one.py m_1_j KaYj` gives `Kan ... KanBiH ... Kansu`, and `python decline_one.py m_1_j viSva-sfj` gives current k-type `viSvasfk ...`.
  - MWinflect #45 open gaps: `KaYj` remains source-gated for B-ending and 7p forms; `viSva-sfj` currently follows PW/MW as k-type but may conflict with an unclear Kale p.97 scan; neuter palatal examples such as `n_1_c su-vfSc` and `n_1_j rAj` are explicitly speculative. Decision: `ready-for-correction` only after those authority checks; do not patch the current palatal code mechanically.
  - MWinflect #46 "Nominals of 1 stem ending in semivowel": `question`, `trivial`, `domain:morphology`. The issue settles `l` from Kale p.54 and `r` from Deshpande p.158; local commands `python decline_one.py m_1_l kamal`, `python decline_one.py n_1_l kamal`, `python decline_one.py f_1_r gir`, and `python decline_one.py n_1_r gir` match the posted tables.
  - MWinflect #46 open gap: final-`v` stems. Local `python decline_one.py m_1_v div` works but contains sparse/empty dual forms exactly as the issue describes from Huet/MW; local `python decline_one.py n_1_v div` raises `IndexError` after `Decline_1_helper ERROR` messages. `nominals/pysanskritv2/inputs/lexnorm-irregular.txt` explicitly parks `janAv`, `dIv`, `pra-div`, `su-div`, and `div` as irregular/unresolved, and currently lists `div` only as `m:f`. Decision: `ready-for-correction` only after authority confirms neuter `div` and defective `janAv`/`dIv` handling; otherwise leave as documented unresolved.
  - MWinflect #48 "Nominals of 1 stem ending in h": `question`, `trivial`, `domain:morphology`. The issue thread already documents settled h-ending classes: `k` type (`duh`, `dah`, `spfh`), `w` type (`lih`, `vAh`, `parIRAh`), optional `k/w` type (`druh`, `muh`, `snuh`, `snih`), `t` type (`upAnah`, `svAruh`), irregular `anaquh`, and irregular `vah` compounds.
  - Local validation confirms settled behavior exists: `python decline_one.py m_1_h duh` produces `Duk ... Dukzu`, matching the issue table.
  - Local evidence for the unresolved gap: `nominals/pysanskritv2/analysis/analyze_1cons.txt` lists `zah` and `sah` compounds; `nominals/pysanskritv2/stems/stems_problem.txt` includes `aBI-zah`, `jalA-zah`, `niz-zah`, and many related h-ending stems.
  - Current generated behavior for `aBI-zah` is short-vowel: masculine `aBIzaw ... aBIzawsu`, neuter `aBIzaw aBIzahI aBIzaMhi`. Issue evidence says MW mentions nom.sg. `zAw` with long `A` for many `zah` compounds, but the issue explicitly leaves unclear which forms receive lengthening.
  - Decision: `ready-for-correction` only after rule-scope research; do not patch all `zah`/`sah` h-ending forms mechanically.

## ✅ Completed (Recent only)

- [x] 2026-06-27: Cloned local `MWinflect` checkout for the non-PWK Cologne question lane.
- [x] 2026-06-27: Researched MWinflect #43 and found cerebral `w/W/q/R` handling locally reproducible while the large `z`/neuter queue remains authority-gated.
- [x] 2026-06-27: Researched MWinflect #44 and found labial declension support exists, while remaining work is authority-gated model assignment for problem stems.
- [x] 2026-06-27: Researched MWinflect #45 and confirmed the palatal code follows documented current choices while `KaYj`, `viSva-sfj`, and speculative neuters remain authority-gated.
- [x] 2026-06-27: Researched MWinflect #46 and confirmed `l`/`r` semivowel models are implemented while `v` neuter/defective forms remain authority-gated.
- [x] 2026-06-27: Researched MWinflect #48 and identified the single unresolved implementation gap: `sah`/`zah` compound long-`A` handling.
